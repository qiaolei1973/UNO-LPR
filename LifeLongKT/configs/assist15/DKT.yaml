cuda_visible_devices: 7

dataset_name: assist15 
model_name: DKT

seed: 42

train:
  num_epochs: 50
  learning_rate: 0.001
  patience: 10
  model_save_path: "./saved_models"

dataloader:
  batch_size: 64
  num_workers: 4
  shuffle: True

dataset:
  max_length: 100

model:
  hidden_size: 200 
  emb_dim: 200 
  dropout: 0.02
  num_layers: 2